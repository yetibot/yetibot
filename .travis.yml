language: clojure

script: lein test

jdk:
  - oraclejdk8

sudo: required

services:
  - docker

before_install:
  - docker login -e="$DOCKER_EMAIL" -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
  - docker version
  - lein docker build
  - lein docker push
